"""Route modules for the Issue Agent HTTP API.

Each submodule owns one domain: analysis (investigation), chat, sessions,
and batch.  ``app.main`` includes these routers on the application instance.
"""
